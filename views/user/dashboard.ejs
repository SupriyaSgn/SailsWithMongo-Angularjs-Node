<!DOCTYPE html>
<html>
  <head>
    <title> | Welcome back, <%= me.name %>!</title>
    <!-- Viewport mobile tag for sensible mobile support -->
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <!--STYLES-->
    <link rel="stylesheet" href="/styles/angular-toastr.css">
    <link rel="stylesheet" href="/styles/bootstrap.3.1.1.css">
    <link rel="stylesheet" href="/styles/angular-bootstrap-calendar.min.css">
    <link rel="stylesheet" href="/styles/bootstrap.css">
    <link rel="stylesheet" href="/styles/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/reset.css">
    <link rel="stylesheet" href="/styles/style.css">
    <link rel="stylesheet" href="/styles/xeditable.css">
    <!--STYLES END-->
    <!--Added this so the client has access to the csrf tag and it's grabbed first service side on every page -->
    <script type="text/javascript">
    window.SAILS_LOCALS = {
      _csrf: '<%= _csrf %>',
      me: <%- JSON.stringify(me) %>
    };
    </script>
  </head>
  <body ng-app="DashboardModule" ng-controller="DashboardController" ng-cloak>

    <!-- container = Fixed-width container container-fluid = full-width -->
    <div class="container-fluid navbar navbar-inverse">
      <!-- Creates the navbar -->
      <nav class=" ">
        <!-- This creates the mechanism for collapse button -->
        <button type="button" class="navbar-toggle" data-toggle="collapse" ng-init="navCollapsed = true" ng-click="navCollapsed = !navCollapsed">
        <!-- This controls the number of lines in the image when the nav collapse -->
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        </button>
        <!-- This is the brand on the left-hand side. -->

        <!-- This is what is the above collapse button points to when the mark-up below is collapsed -->
        <div>
          <div class="collapse navbar-collapse" ng-class="!navCollapsed && 'in'">
          <!-- This is what "styles" the li's as navbar items -->
          <ul class="nav navbar-nav" >
            <li ng-if="me.isAdmin" ng-class="{'active':getClass('/user')}">
              <a href="/user">User Administration</a>
            </li>
            <li>
              <li><a  ui-sref="dashboard">Dashboard</a></li>
            </li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="gravatarSm"><img ng-src="{{me.gravatarUrl}}" width="24" height="24"/></li>
            <li class="activityOverlord active">
              <a href="#/profile" class="pointer"> {{me.firstname}}</a>
            </li>
            <!-- <li class="activityOverlord active"><a href="/{{id}}">{{userName}}</a></li> -->
            <!-- navbar-right pulls this element right -->
            <!-- Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller
            fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup
            controller will not be tied to this element. -->
            <li>
              <a href="/logout">sign-out</a>
            </li>
            <!-- <a ng-controller="signinController" class="btn btn-default navbar-btn navbar-right" href="#" ng-if="!isAuthenticated">sign-in</a> -->
            <!-- Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller
            fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup
            controller will not be tied to this element. -->
          </ul>
        </div>
          <!--<div class="collapse navbar-collapse" ng-class="!navCollapsed && 'in'">-->
            <!--&lt;!&ndash; This is what "styles" the li's as navbar items &ndash;&gt;-->
            <!--<ul class="nav navbar-nav" >-->
              <!--<li ng-show="me.isAdmin" ng-class="{'active':getClass('/user')}">-->
                <!--<a href="/user">User Administration</a>-->
              <!--</li>-->
              <!--<li>-->
                <!--<a href="/demo">You can put more stuff here</a>-->
              <!--</li>-->
            <!--</ul>-->
            <!--<ul class="nav navbar-nav navbar-right">-->
              <!--<li class="gravatarSm"><img ng-src="{{me.gravatarUrl}}" width="24" height="24"/></li>-->
              <!--<li class="activityOverlord active">-->
                <!--<a href="/demo" class="pointer"> {{me.firstname}}</a>-->
              <!--</li>-->
              <!--&lt;!&ndash; <li class="activityOverlord active"><a href="/{{id}}">{{userName}}</a></li> &ndash;&gt;-->
              <!--&lt;!&ndash; navbar-right pulls this element right &ndash;&gt;-->
              <!--&lt;!&ndash; Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller-->
              <!--fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup-->
              <!--controller will not be tied to this element. &ndash;&gt;-->
              <!--<li>-->
                <!--<a href="/logout">sign-out</a>-->
              <!--</li>-->
              <!--&lt;!&ndash; <a ng-controller="signinController" class="btn btn-default navbar-btn navbar-right" href="#" ng-if="!isAuthenticated">sign-in</a> &ndash;&gt;-->
              <!--&lt;!&ndash; Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller-->
              <!--fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup-->
              <!--controller will not be tied to this element. &ndash;&gt;-->
            <!--</ul>-->
          <!--</div>-->
          <!-- This is what "styles" the li's as navbar items -->
       <!--    <ul class="nav navbar-nav navbar-right">
            <!-- navbar-right pulls this element right -->
            <!-- Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller
            fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup
            controller will not be tied to this element. -->
                        <!-- <li><a href="#"></a></li>
			<li><a href="#attendance">Attendence</a></li>
			<li> <a href="#profile">Student Profile</a></li>
                        <li><a href="/user"> List Of Student</a></li>
			<li> <a href="#bus">Bus Profile</a></li>
			<li><a href="#pushsms">Push SMS</a></li>
            <li><a href="/logout">Logout</a></li> -->
            <!-- <a ng-controller="signinController" class="btn btn-default navbar-btn navbar-right" href="#" ng-if="!isAuthenticated">sign-in</a> -->
            <!-- Using ng-show instead of ng-if because we need this mark-up to be in the dom when signup controller
            fires.  If it's not there (which it wouldn't be in the case of ng-if) then the scope of the signup
            controller will not be tied to this element. -->
          <!-- </ul>
          <div ng-view></div> -->

        </div>
      </nav>




    </div>
    <div class="row">
    <div class="col-sm-2">
       <div class="navbar-default sidebar" role="navigation">
        <div class="sidebar-nav navbar-collapse">
       <ul class="nav" id="side-menu">
                        <li class="sidebar-search">
                            <div class="input-group custom-search-form">
                                <input type="text" class="form-control" placeholder="Search...">
                                <span class="input-group-btn">
                                <button class="btn btn-default" type="button">
                                    <span class="glyphicon glyphicon-search"></span></i>
                                </button>
                            </span>
                            </div>
                            <!-- /input-group -->
                        </li> <!-- sidebar search -->
        <li><a  ui-sref="dashboard"><span class="glyphicon glyphicon-dashboard"></span> Dashboard</a></li>
        <li><a  ui-sref="schedulerTab.scheduler"><span class="glyphicon glyphicon-calendar"></span> Schedular</a></li>
        <li><a  ui-sref="patient"><span class="glyphicon glyphicon-user"></span> Patient</a></li>
        <li><a  ui-sref="emr.notepad" data-ng-class="{ active: $state.includes('emr.notepad') }"><span class="glyphicon glyphicon-record"></span> EMR</a></li>
        <li><a  ui-sref="clinicTab.basicinfo"><span class="glyphicon glyphicon-leaf"></span> Clinic</a></li>
        <li><a  ui-sref="pathLab"><span class="glyphicon glyphicon-tint"></span> Path Labs</a></li>
        <li><a  ui-sref="data"><span class="glyphicon glyphicon-briefcase"></span> Data</a></li>
        <li><a  ui-sref="emr.ePrescription"><span class="glyphicon glyphicon-pencil"></span> e-Prescription</a></li>
        <li><a  ui-sref="pharmacy"><span class="fa fa-medkit"></span> Pharmacy</a></li>
        <li><a  ui-sref="billingSideMenu"><span class="fa fa-inr"></span> Billing</a></li>     
      </ul>  <!-- side menu -->
        </div> <!--sidebar second -->
 </div>
    </div>
    <div class="col-sm-10">
    <div ui-view> </div>
      
    </div>
      
    </div>
    

    <!--SCRIPTS-->
    <script src="/js/dependencies/sails.io.js"></script>
    <script src="/js/dependencies/angular.js"></script>
    <script src="/js/dependencies/jquery.min.js"></script>
    <script src="/js/dependencies/angular-animate.js"></script>
    <script src="/js/dependencies/angular-bootstrap-calendar-tpls.min.js"></script>
    <script src="/js/dependencies/angular-route.js"></script>
    <script src="/js/dependencies/angular-toastr.js"></script>
    <script src="/js/dependencies/angular-ui-router.min.js"></script>
    <script src="/js/dependencies/angular-ui.js"></script>
    <script src="/js/dependencies/bootstrap.js"></script>
    <script src="/js/dependencies/compareTo.module.js"></script>
    <script src="/js/dependencies/ui-bootstrap-tpls.min.js"></script>
    <script src="/js/dependencies/xeditable.js"></script>
    <script src="/js/homepage/homepageController.js"></script>
    <script src="/js/private/dashboard/dashboardController.js"></script>
    <script src="/js/signup/SignupController.js"></script>
    <!--SCRIPTS END-->

  </body>
</html>
